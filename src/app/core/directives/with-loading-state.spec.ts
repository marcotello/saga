import { WithLoadingState } from './with-loading-state';

describe('WithLoadingState', () => {
  it('should create an instance', () => {
    const directive = new WithLoadingState();
    expect(directive).toBeTruthy();
  });
});
